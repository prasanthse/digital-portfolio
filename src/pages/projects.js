import { Helmet } from "react-helmet";

const ProjectsPage = () => {
    <Helmet>
        <title>Projects</title>
    </Helmet>

    return (
        <>
        
        </>
    );
}

export default ProjectsPage;