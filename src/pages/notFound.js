import { Helmet } from "react-helmet";

const NotFound = () => {
    <Helmet>
        <title>Not Found</title>
    </Helmet>

    return(
        <>
            <h1>Not found page</h1>
        </>
    );
}

export default NotFound;